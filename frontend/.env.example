# Local dev (default)
VITE_API_URL=http://localhost:8000
VITE_ENABLE_POWERSYNC=true

# Vercel production — set in project env (recommended) or rely on vercel.app fallback in src/api/base-url.ts:
# VITE_API_URL=https://canine-scrambled-reseller.ngrok-free.dev
#
# Backend: run ./scripts/ngrok-all.sh (API + PowerSync tunnels), then restart compose.

# VITE_SYNC_DEBUG=false
